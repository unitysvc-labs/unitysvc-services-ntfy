# .env.example — the seller config this service needs.
# `usvc_seller secrets upload .env.example` seeds the value (for testing) and
# the description (unitysvc/unitysvc#1618). The value uses ${NAME:-default}: the
# real value comes from the environment (a GitHub Actions secret/variable in
# CI), else the default. Also sourceable locally: `set -a; . ./.env.example`.

# Base URL of the ntfy server that receives published notifications. The
# gateway appends each enrollment's 6-character code as the private topic, so
# this is the server root with no trailing slash.
NTFY_SERVER_URL=${NTFY_SERVER_URL:-https://ntfy.unitysvc.dev}
